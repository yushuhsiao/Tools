using System.Data;
using Dapper.Contrib.Extensions;

namespace Dapper.Contrib
{
    public static class SqlMapperExtensions
    {
        //public static long Insert<T>(this IDbTransaction transaction, T entityToInsert, int? commandTimeout = null)
        //    => transaction.Connection.Insert<T>(transaction.Connection, entityToInsert, transaction, commandTimeout);
    }
}
